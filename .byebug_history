exit
category_hash
exit
category_hash
categories.each do |category| category_hash.append(category.extract!("id","name")) end
category_hash
exit
cache_post_id
post_id
categories.each do |cat| p cat.extract!("id","name") end
categories.class
categories.each do |cat| p cat["id"] end
categories
category_hash
exit
cache_posts.nil?
cache_post_id
exit
cache_posts.slice(["id"])
cache_posts.slice!(["id"])
cache_posts.slice!(*["id"])
cache_posts
posts
cache_posts.slice!(["id"])
cache_post_id = cache_posts[0].slice!(["id"])
cache_post_id
cache_post_id = cache_posts[0].slice!("id")
cache_posts
cache_post_id
cache-post_id
post_id
continue
