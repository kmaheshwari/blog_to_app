exit
cache_posts.nil?
cache_post_id
exit
cache_posts.slice(["id"])
cache_posts.slice!(["id"])
cache_posts.slice!(*["id"])
cache_posts
posts
cache_posts.slice!(["id"])
cache_post_id = cache_posts[0].slice!(["id"])
cache_post_id
cache_post_id = cache_posts[0].slice!("id")
cache_posts
cache_post_id
cache-post_id
post_id
continue
